html
  head
    titleScreenInvader Options/title
  
  body
    div
      label(for="ip") the ip-address or url you access the screeninvader from:
      input#ip(type="text")
      label(for="ip") no trailing slash (/ after the url) and no http:// please.

    div
      label(for="switchToTabOnLoad") switch to screeninvader when i click the button:
      input#switchToTabOnLoad(type="checkbox")
    
    br
    div#status
      input#save(type="button", value="Save")

    script(src="options.js")
